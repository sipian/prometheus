workspace:
    base: /go
    path: src/github.com/prometheus/prometheus
pipeline:
  test:
    image: sipian/golang-builder:1.10-base
    environment:
      - PLUGIN_PATH=github.com/prometheus/prometheus
    commands:
      - ls
matrix:
  TAG:
    - 1.10
